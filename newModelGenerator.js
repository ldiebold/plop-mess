let newModelPrompts = require('./newModelPrompts')
let newModelActions = require('./newModelActions')

module.exports = {
  description: 'New Creator',
  prompts: newModelPrompts,
  actions: newModelActions,
}